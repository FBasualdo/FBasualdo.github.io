/*function ampliarProyecto(element) {
  // Obtener todos los elementos de proyecto
  const proyectos = document.querySelectorAll('.project-div');
  element.classList.add('expands')
    // Quitar la clase ampliado de los otros proyectos
  proyectos.forEach((proyecto) => {
    if (proyecto !== element) {
      proyecto.classList.remove('expands');
      proyecto.classList.add('rest')
    }
  });
}

function mouseout() {
const proyectos = document.querySelectorAll('.project-div');
proyectos.forEach((proyecto) => {
  proyecto.classList.remove('expands');
  proyecto.classList.remove('rest')
  });
}
*/